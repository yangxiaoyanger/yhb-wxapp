<map id="map" longitude="{{info.lon}}" latitude="{{info.lat}}" scale="14" controls="{{controls}}" bindcontroltap="controltap" markers="{{markers}}" bindmarkertap="markertap" polyline="{{polyline}}" circles="{{circles}}" bindregionchange="regionchange" show-location style="width: 100%; height: 530px;">
</map>
<view class='mainView'>

    <view class='view1'>{{info.name}}</view>
    <view class='view2'>{{info.address}}</view>
</view>



